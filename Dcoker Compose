version: '3.8'

services:
  frontend:
    build: ./frontend-html
    ports:
      - "80:80"
    depends_on:
      - backend

  backend:
    build: ./backend-flask
    ports:
      - "5000:5000"
    environment:
      MYSQL_HOST: database
      MYSQL_USER: root
      MYSQL_PASSWORD: password
      MYSQL_DATABASE: appdb
    depends_on:
      - database

  database:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: appdb
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
